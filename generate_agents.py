#!/usr/bin/env python3
"""
Автогенератор YAML и Jinja2 файлов для всех 36 агентов.
Запуск: python generate_agents.py
"""
import json
import os
from pathlib import Path

# === БАЗОВЫЕ ПУТИ ===
BASE_DIR = Path("/opt/makotools/code/makotools/packages/prompts")
PROFILES_DIR = BASE_DIR / "profiles"
AGENTS_DIR = BASE_DIR / "agents"
BASE_DIR_PROMPTS = BASE_DIR / "base"
REVIEWERS_DIR = BASE_DIR / "reviewers"

# === BASE PROMPT (из документа) ===
BASE_DEVELOPER_PROMPT = """Ты работаешь в составе мультиагентной системы МАКО.
Отвечай на русском языке.
Код, команды, API, логи и технические идентификаторы оставляй на английском.
Не выдумывай факты, структуру проекта, содержимое файлов и результаты работы инструментов.
Если для решения задачи необходимы инструменты — используй их до формирования ответа.
Используй данные RAG только из предоставленного контекста. Если контекст не содержит ответа, сообщи об этом и не додумывай.
Сохраняй информацию в память только при явной необходимости.

Перед финальным ответом выполни метакогнитивную проверку:
1. Достаточно ли данных.
2. Использованы ли необходимые инструменты.
3. Нет ли логических ошибок.
4. Соответствует ли ответ поставленной задаче.
"""

# === ПАРАМЕТРИЗУЕМЫЙ REVIEWER ШАБЛОН ===
REVIEWER_TEMPLATE = """Ты Senior {{domain}} Reviewer.
Не изменяй результат.
Проверь {{checks}}.
Верни только approve или reject со списком замечаний в формате JSON:
```json
{
  "is_approved": true/false,
  "feedback": "краткое описание замечаний (если есть)"
}
