Процедура ПриОткрытии(Отказ)
    // При открытии обработки
КонецПроцедуры

Процедура Выгрузить(Команда)
    // Получаем остатки товаров
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ Товары.Ссылка, Товары.Наименование, ОстаткиТоваров.КоличествоОстаток ИЗ РегистрНакопления.ОстаткиТоваров КАК ОстаткиТоваров
                   |ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Товары КАК Товары ПО ОстаткиТоваров.Товар = Товары.Ссылка";
    
    Результат = Запрос.Выполнить();
    Выборка = Результат.Выбрать();
    
    // Подготавливаем CSV
    CSVСтрока = "Код;Наименование;Остаток" + Символы.ПС;
    
    Пока Выборка.Следующий() Цикл
        CSVСтрока = CSVСтрока + Выборка.Ссылка.Код + ";" + Выборка.Наименование + ";" + Выборка.КоличествоОстаток + Символы.ПС;
    КонецЦикла;
    
    // Сохраняем в файл
    Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Сохранение);
    Диалог.Заголовок = "Выберите файл для сохранения остатков";
    Диалог.Фильтр = "CSV файлы|*.csv";
    Если Диалог.Выбрать() Тогда
        ИмяФайла = Диалог.ПолноеИмяФайла;
        Если Правая(ИмяФайла, 4) <> ".csv" Тогда
            ИмяФайла = ИмяФайла + ".csv";
        КонецЕсли;
        
        Попытка
            ЗаписьТекста = Новый ЗаписьТекста(ИмяФайла, КодировкаТекста.UTF8);
            ЗаписьТекста.Записать(CSVСтрока);
            ЗаписьТекста.Закрыть();
            Сообщить("Файл успешно сохранен: " + ИмяФайла);
        Исключение
            Сообщить("Ошибка при сохранении файла: " + ОписаниеОшибки());
        КонецПопытки;
    КонецЕсли;
КонецПроцедуры